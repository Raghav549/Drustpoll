import { applyWebhook, verifyHmacSha256, type PaymentProvider } from './payment-service.js';

export async function handleVerifiedPaymentWebhook(provider:PaymentProvider,rawBody:string,signature:string){if(!rawBody||rawBody.length>1024*1024)throw new Error('Invalid webhook body');if(!verifyHmacSha256(rawBody,signature,process.env.PAYMENT_WEBHOOK_SECRET??''))throw new Error('Invalid webhook signature');return applyWebhook(provider,rawBody,signature);}

export function makeHmacPaymentProvider(name:string,transport:{createIntent(input:{orderId:string;amountMinor:number;currency:string;idempotencyKey:string}):Promise<{providerIntentId:string;status:'created'|'requires_action'|'processing'}>;parseWebhook(rawBody:string):{eventId:string;type:string;providerIntentId:string;status:'succeeded'|'failed'|'cancelled'}}):PaymentProvider{if(!name)throw new Error('Payment provider is not configured');return{name,createIntent:transport.createIntent,verifyWebhook({rawBody}){return transport.parseWebhook(rawBody);}};}
