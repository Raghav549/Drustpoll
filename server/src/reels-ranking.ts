import { query } from './db.js';
export { diversifySlate, watchAffinity, type ReelRankItem } from './reels-ranking-pure.js';
export async function recordSlateExposure(userId:string,surface:'feed'|'reels'|'shop',slate:Array<{postId:string;creatorId:string}>){if(!slate.length)return 0;const bounded=slate.slice(0,100).filter(x=>x.postId&&x.creatorId);if(!bounded.length)return 0;const values:string[]=[];const params:unknown[]=[userId,surface];bounded.forEach((x,i)=>{const base=params.length;values.push(`($1,$${base+1},$${base+2},$2,$${base+3},now())`);params.push(x.postId,x.creatorId,i);});const r=await query(`INSERT INTO recommendation_exposure(user_id,post_id,creator_id,surface,position,shown_at) VALUES ${values.join(',')} ON CONFLICT DO NOTHING`,params);return r.rowCount;}
